export enum SelectedPage {
    Home = "home",
    OurMenu = "ourmenu",
    ContactUs = "contactus",
  }
  
  export interface ClassType {
    name: string;
    description?: string;
    image: string;
  }